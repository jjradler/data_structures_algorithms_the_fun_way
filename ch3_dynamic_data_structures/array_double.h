#ifndef ARRAY_DOUBLE_H
#define ARRAY_DOUBLE_H

/*!
 * Return an pointer to an array double the length of the original
 * with original values copied into the first N slots.
 */
int* arrayDouble(int *A, int arraySize);

#endif /*ARRAY_DOUBLE_H*/
